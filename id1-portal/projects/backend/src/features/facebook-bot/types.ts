export type page_client = {
    pageID: number,
    pageAccessToken: string
}
export type news_for_facebook = {
    title: string,
    description: string,
    news_URL: string,
    main_picture: string
}